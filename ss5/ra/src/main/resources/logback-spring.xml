<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true">

    <!-- Console log -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level [%thread] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- File log xoay vòng -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <!-- File log chính -->
        <file>logs/app.log</file>

        <!-- Chính sách xoay log: vừa theo ngày vừa theo dung lượng -->
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!-- Mẫu tên file log: logs/app-2025-09-15.0.log -->
            <fileNamePattern>logs/app-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <!-- Giữ log trong 30 ngày -->
            <maxHistory>30</maxHistory>
            <!-- Giới hạn dung lượng mỗi file log -->
            <maxFileSize>10MB</maxFileSize>
            <!-- Giới hạn tổng dung lượng tất cả file log -->
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>

        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level [%thread] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Root logger -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

    <!-- Cấu hình log riêng cho package -->
    <logger name="com.example.demo" level="DEBUG"/>
    <logger name="org.springframework.web" level="INFO"/>
    <logger name="org.hibernate.SQL" level="DEBUG"/>
    <logger name="org.hibernate.type.descriptor.sql.BasicBinder" level="TRACE"/>

</configuration>